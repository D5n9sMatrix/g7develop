'window'
'active'
'layers'

$window(function(win, gib, manager){

     if(!win == this.gib != manager)
     {
         for(var win = 10; win < 100; win++)
         {
              win += 10;
              log.write(win++);
         }
     } else {
       return win;
     }

});

$active(function(win, gib, manager){

     if(!win == this.gib != manager)
     {
         for(var win = 10; win < 100; win++)
         {
              win += 10;
              log.write(win++);
         }
     } else {
       return win;
     }

});
$layers(function(win, gib, manager){

     if(!win == this.gib != manager)
     {
         for(var win = 10; win < 100; win++)
         {
              win += 10;
              log.write(win++);
         } else {
           return win;
         }
     }

});
