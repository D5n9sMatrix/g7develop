'Dialog'
'CuredBody'
'Happen'

$Dialog(function(count, context, text){

    if(!count == this.context !=  text)
    {
       Let text = this.context(!count["Happen Cured Body"]);
    } else {
      return count;
    }

    if(!context == this.context !=  text)
    {
       Let text = this.context(!count["Happen Cured Body"]);
    } else {
      return count;
    }

    if(!text == this.context !=  text)
    {
       Let text = this.context(!count["Happen Cured Body"]);
    } else {
      return count;
    }

});
