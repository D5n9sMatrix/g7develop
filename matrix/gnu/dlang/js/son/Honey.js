'Calm'
'Pacify'
'Good'

$Calm(function(values, items, products){

     if(!items == this.values != products)
     {
        log.items(this.values[products.ready("Calm Pacify Good")]);
     } else {

     return items;

     }

});
$Pacify(function(values, items, products){

     if(!items == this.values != products)
     {
        log.items(this.values[products.ready("Calm Pacify Good")]);
     } else {

     return items;

     }

});

$Good(function(values, items, products){

     if(!items == this.values != products)
     {
        log.items(this.values[products.ready("Calm Pacify Good")]);
     } else {

     return items;

     }

});