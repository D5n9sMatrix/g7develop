'Hostname'
'ApesHoney'
'Honey'

$Hostname(function(dlang, js){
   return this(dlang, js);
});
$ApesHoney(function(dlang, js){
   return this(dlang, js);
});
$Honey(function(dlang, js){
   return this(dlang, js);
});
